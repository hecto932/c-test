// Triangle area
// area = base * altura / 2
// %g remove the zeros

#include <stdio.h>

int main() {
	float base, altura, area;

	base = 5.25;
	altura = 17.32;
	area = (base * altura) / 2;

	printf("Area: %g \n", area);
}